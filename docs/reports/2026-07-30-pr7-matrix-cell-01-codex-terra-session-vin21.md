# PR #7 — Cell 01: Codex / GPT-5.6 Terra High / direct session

The clean official matrix starts with `VIN-21`, created from the real Android App on the real paired Symphony Host. It uses Codex CLI with `gpt-5.6-terra` and `high` selected explicitly, opens direct session `#23`, and is limited to the `website/` repository of the multi-repository `vinext-health` workspace.

The final durable Host run is `20260731025115-1334275` and passed:

- focused Playwright `/health` E2E: 2 passed, with desktop and mobile captures;
- `npm run build`: passed;
- Android review: latest passed evidence record, report, image viewer, video and trace opened through encrypted RPC.

All binary artifacts, manifests, trace, logs, provenance and SHA-256 hashes are in the dedicated [Cell 01 Gist](https://gist.github.com/raphaelcangucu/f9d8fe2a9f491853149ad26738e5b553). No binary evidence is stored under `docs/pr-assets`.

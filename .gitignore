# Git worktrees for isolated agent runs
.worktrees/

# Local backup of refactor phase 5a modules (not part of this branch)
.phase5a-recovered/

# Per-developer project YAML bundles (make project-import / project-export)
/projects/

# Expo routes named "projects" are source code, not local project exports.
!/mobile/app/h/[[]hostId[]]/projects.tsx
!/mobile/app/h/[[]hostId[]]/projects/
!/mobile/app/h/[[]hostId[]]/projects/**

.superpowers/
